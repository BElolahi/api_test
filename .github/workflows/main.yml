name: End-to-end tests
on: [push]
jobs:
  cypress-test:
    timeout-minutes: 60
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
      - uses: actions/checkout@v2
        with:
          node-version: lts/*
      - name: Cypress run
        uses: cypress-io/github-action@v2